
let retry = 0;
do {
    console.log("Execute a code!");
    console.log("Retrying.....", retry);
    retry++;
} while (retry < 3);

/* let retry1 = 0;
while (retry1 < 3) {
    console.log("Execute a code!");
    console.log("Retrying.....", retry1);
    retry1++;
} */